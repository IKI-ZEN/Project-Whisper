{
  "name": "project-whisper",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/lib/utils.test.ts src/lib/analysis.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
